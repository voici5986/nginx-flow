# Google Analytics 4 配置
# 替换为你的 Measurement ID (格式: G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=G-57QM27LHEM

# 是否在开发环境启用 GA4 (可选，默认关闭)
# VITE_GA_ENABLE_DEV=true

# 是否启用 Cookie 同意横幅 (可选，默认开启)
# 设置为 false 后：不显示横幅，但依然收集 GA4 数据（适合中国用户）
VITE_ENABLE_COOKIE_CONSENT=false
